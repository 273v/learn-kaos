---
title: Your first example
description: Redline two contract versions in one command — see the tracked changes, then accept or reject them. Deterministic, offline, no API key.
---

import { Code, CardGrid, LinkCard } from '@astrojs/starlight/components';
import redlineSrc from '#examples/redline-contract.py?raw';

Let's run KAOS on a task every lawyer knows: **redlining a contract**. This example compares
two versions of an agreement, produces a Word-style **redline** (tracked changes), and then
**accepts or rejects** them in code — using [`kaos-office`](https://github.com/273v/kaos-office)
and [`kaos-content`](https://github.com/273v/kaos-content). It's fully **deterministic** and
**offline**: no API key, no model download, same output every time.

## Run it

One command — no clone, no virtualenv, no key:

```bash
uv run https://raw.githubusercontent.com/273v/learn-kaos/main/examples/redline-contract.py
```

Or from a local clone of this repo:

```bash
uv run examples/redline-contract.py
```

You'll see:

```text
4 tracked change(s):

   deleted: 'five'  (by Reviewer)
  inserted: 'seven'  (by Reviewer)
   deleted: 'three'  (by Reviewer)
  inserted: 'five'  (by Reviewer)

  accept all -> The interest rate is seven percent and the term is five years.
  reject all -> The interest rate is five percent and the term is three years.
```

Counsel changed the rate (five → seven percent) and the term (three → five years); KAOS
surfaces each edit as a **typed tracked change**, and resolves them two ways.

## The code

This is the exact file that runs above — and the exact file this site's CI runs on every
change. What you read here cannot drift from what's tested, because the page imports the file
byte-for-byte:

<Code code={redlineSrc} lang="python" title="examples/redline-contract.py" mark={['ko.write_redline(original, revised, redline)', 'Revisions.from_document(doc)']} />

## What just happened

- The `# /// script` block at the top is [PEP 723](https://peps.python.org/pep-0723/) inline
  metadata. `uv` reads it, resolves the packages into a cached isolated environment, and runs
  the file — that's why one command is enough.
- **`write_redline(v1, v2, out)`** compares two DOCX versions into a real Word redline. Open
  it in Word and it looks hand-marked.
- **`parse_docx(path, track_changes=True)`** then **`Revisions.from_document(doc)`** surfaces
  each edit as a *typed* `Revision` — its `change_type` (insertion/deletion), `text`, and
  `author` — so you can review changes in code, not by eyeballing.
- **`accept_all`** / **`reject_all`** resolve the markup: accepting yields the revised
  version, rejecting restores the original. The `assert`s at the bottom make the file **its
  own test**, so this page is guaranteed correct.

## Next

<CardGrid>
  <LinkCard
    title="Start the golden path →"
    href="/learn-kaos/tutorials/first-tool"
    description="Author a tool, build a document, call a model, build an agent — each step runnable offline."
  />
  <LinkCard
    title="Turn a contract into a template"
    href="/learn-kaos/how-to/contract-to-template"
    description="Find a contract's parties, amounts & dates with on-device NER and swap them for reusable fields."
  />
  <LinkCard
    title="Browse the gallery"
    href="/learn-kaos/gallery"
    description="Every one-command example, most fully offline, no API key."
  />
  <LinkCard
    title="The bigger picture"
    href="/learn-kaos/what-is-kaos"
    description="How the kaos-* packages fit together into one composable stack."
  />
</CardGrid>
