---
title: Learn KAOS
description: Tested, multi-learner docs and one-command runnable examples for the open-source Kelvin Agentic Operating System (KAOS).
template: splash
hero:
  tagline: Open agentic infrastructure for legal and financial work — learn it by running it. Every example on this site is a single command, and every snippet is tested.
  image:
    html: |
      <div class="hero-code-card">
        <div class="hero-code-head"><span></span><span></span><span></span><span class="hero-code-file">contract-to-template.py</span></div>
        <pre class="hero-code-body"><code><span class="hcl-prompt">$</span> uv run contract-to-template.py

      Found 4 fields with on-device NER (no key):
        <span class="hcl-f">{{date_1}}</span>   &lt;- 'March 1, 2026'
        <span class="hcl-f">{{party_1}}</span>  &lt;- 'Acme Corporation'
        <span class="hcl-f">{{party_2}}</span>  &lt;- 'Globex Industries'
        <span class="hcl-f">{{amount_1}}</span> &lt;- '$50,000'

      Redline: 4 fields, 8 tracked changes &rarr; .docx</code></pre>
      </div>
  actions:
    # Hero actions are frontmatter (not page content), so the base path is not
    # auto-prepended — include it explicitly here.
    - text: Run your first example
      link: /learn-kaos/get-started/first-example
      icon: right-arrow
    - text: What is KAOS?
      link: /learn-kaos/what-is-kaos
      icon: open-book
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<p class="eyebrow">Get started</p>

## Pick your path

<CardGrid>
	<Card title="Just show me it works" icon="rocket">
		Run a deterministic sample in ~10 seconds — no API key, no setup.
		Start with [your first example](/get-started/first-example), then browse
		the [gallery](/gallery).
	</Card>
	<Card title="Teach me, step by step" icon="open-book">
		Follow the guided tutorial spine from [What is KAOS?](/what-is-kaos)
		onward — each tutorial builds on the last, all runnable offline.
	</Card>
	<Card title="I have a task to do" icon="wrench">
		Jump to a [how-to recipe](/how-to/extract-citations) — SQL over billing
		data, extract contract definitions, rank a graph, verify a citation.
	</Card>
	<Card title="I want the facts" icon="list-format">
		[Reference](/reference/packages) for every package — the package map, CLI,
		glossary, citation kinds, graph algorithms, compatibility.
	</Card>
</CardGrid>

<p class="eyebrow">A real example</p>

## See it run

Here's the kind of task KAOS is built for: turn a *signed contract* into a **reusable
template**. One command reads the PDF, finds the parties, amounts, and dates with an
on-device model (no API key), swaps them for template fields, and redlines the changes:

```bash
uv run examples/contract-to-template.py
```

```text
Found 4 field(s) with on-device NER (no LLM, no key):
  {{date_1}}   <- 'March 1, 2026'
  {{party_1}}  <- 'Acme Corporation'
  {{party_2}}  <- 'Globex Industries'
  {{amount_1}} <- '$50,000'

Redline (contract → template): 4 fields parameterized, 8 tracked change(s) → redline.docx
```

That's the pattern: **56 tested examples**, each one `uv run` away, most fully offline.
[See how it works →](/how-to/contract-to-template) · [browse them all →](/gallery)

<p class="eyebrow">The approach</p>

## Why this site is different

- **One command per example.** Every sample is a self-contained
  [PEP 723](https://peps.python.org/pep-0723/) script — `uv run examples/<name>.py`,
  no clone, no virtualenv juggling.
- **Tested, not aspirational.** The code you see is imported byte-for-byte from a
  file that runs in CI. If a snippet would break, the build fails.
- **Offline by default.** LLM examples use a deterministic in-process fake model,
  so they run for free with no API keys — and switch to a real provider with one
  environment variable.
